
# test files

sudo: false

install:
- npm install -g recess stylus
- npm install
- pip install --user docutils

script:
- grunt
- mkdir -vp build/html
- rst2html.py ReadMe.rst build/html/ReadMe.html

branches:
  only:
  - test
  - '/^[0-9]*\.[0-9]*\.[0-9]*/'

